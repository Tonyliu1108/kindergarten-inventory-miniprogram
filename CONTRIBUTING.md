# 参与贡献

感谢参与改进。提交变更前请：

1. 从最新默认分支创建功能分支。
2. 不要提交真实 AppID、云环境 ID、openid、凭据、用户数据或本地配置。
3. 对所有新增云函数先做身份与角色校验，并只返回前端需要的字段。
4. 对文本、数字、文件路径和批量数量设置明确边界。
5. 对变更过的 JavaScript 运行 `node --check`，并在微信开发者工具中完成对应流程的手工验证。
6. 在 Pull Request 中说明安全影响、测试范围和仍需人工验证的部分。

安全漏洞请按 [SECURITY.md](SECURITY.md) 私下报告，不要提交公开 Issue。
