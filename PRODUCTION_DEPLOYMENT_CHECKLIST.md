# 真实环境上线检查清单

本清单适用于把本仓库部署到微信小程序和腾讯云开发真实环境。它用于记录发布证据，
不能代替微信公众平台、腾讯云开发控制台的当前规则或组织内部的合规审查。

真实 AppID、云环境 ID、openid、管理员姓名、确认口令和采购凭证不得填写在本文件、
Git 提交、公开 Issue、Actions 日志或截图中。需要留档时，只保存在组织批准的私密系统。

## 1. 发布范围与责任

- [ ] 明确本次发布是测试、灰度还是生产，并为三者使用不同的云环境。
- [ ] 指定发布负责人、安全复核人、业务验收人和故障回滚负责人。
- [ ] 记录准备发布的 Git 提交 SHA；确认来源是受保护的 `main` 分支。
- [ ] 确认 GitHub 的 Security checks 和 CodeQL 均成功，Secret Scanning、
      Code Scanning 和高危 Dependabot 告警均为 0。
- [ ] 确认没有把旧私有归档、历史备份包或本地配置上传到部署目录。

本地发布前至少执行：

```bash
git status --short
git rev-parse HEAD
git ls-files | grep -E '(^|/)(config\.local\.js|project\.private\.config\.json|\.env(\.|$))'
```

第一条命令应为空；第三条命令也不应输出任何文件。

## 2. 账号与环境隔离

- [ ] 使用组织控制的微信公众平台和腾讯云账号，不使用不可交接的个人账号作为唯一所有者。
- [ ] 为平台所有者和关键管理员启用多因素认证，并确认恢复方式可用。
- [ ] 测试与生产使用不同的云环境、数据库、云存储和函数配置。
- [ ] 生产环境只授予完成发布所需的最小控制台权限，发布后回收临时权限。
- [ ] 配置费用预算、用量告警和资源配额，避免异常调用产生不可控费用。

## 3. 本机配置

- [ ] 从 `miniprogram/config.local.example.js` 创建被忽略的
      `miniprogram/config.local.js`，只在本机填写目标云环境 ID。
- [ ] 在微信开发者工具中填写真实 AppID；本地差异只保存在被忽略的
      `project.private.config.json`。
- [ ] 确认 `project.config.json` 仍保留公开模板值，没有真实 AppID。
- [ ] 确认生产环境没有设置 `ALLOW_TEST_DATA_RESET=true`，也没有设置
      `RESET_TEST_DATA_CONFIRMATION`。
- [ ] 不在云函数代码中保存密钥；必须使用的配置放入云函数环境变量，并限制可读取人员。

## 4. 数据库、存储与云函数

- [ ] 在目标环境创建 `users`、`user_applications`、`item_skus`、
      `stock_records`、`purchase_receipts` 五个集合。
- [ ] 对每个集合应用 `database-security-rules.example.json` 的拒绝客户端直连基线。
- [ ] 对云存储应用 `storage-security-rules.example.json`，验证文件类型、10 MiB
      上限、上传者读取边界和导出文件读取边界。
- [ ] 如果需要多管理员共享采购凭证，先设计并验证独立授权模型；不得把目录改为公开读取。
- [ ] 对云函数应用 `cloudfunction-security-rules.example.json`，拒绝匿名调用。
- [ ] 部署全部需要的云函数，并使用各函数已提交的 `package-lock.json` 安装云端依赖。
- [ ] 确认云端实际安装的是 `wx-server-sdk@4.0.2`、`ws@8.21.1`，以及通过
      `lodash.set` 路径安装的 `lodash@4.18.1`。
- [ ] 不启用数据库实时 `watch`；若业务需要该能力，先进行单独依赖和权限审计。
- [ ] 为 `cleanupTempPurchaseReceipts` 创建名为
      `cleanupTempPurchaseReceiptsTimer` 的定时触发器，并确认客户端无法直接调用。
- [ ] 生产环境不部署 `resetTestData`，或确认其两个启用环境变量均不存在。

## 5. 首位管理员与权限验收

- [ ] 先由预定管理员本人在小程序提交申请，再从控制台申请记录中取得 openid。
- [ ] 只在云数据库控制台创建首位 `active` 管理员，不把 openid 复制到代码或文档。
- [ ] 使用四类测试身份分别验收：未申请用户、待审核用户、老师、管理员。
- [ ] 验证未开通用户只能提交申请，不能读取库存或调用业务云函数。
- [ ] 验证老师只能查看库存、出库、换码和查看自己的记录。
- [ ] 验证管理员可以入库、盘点、维护物品、审核用户、管理凭证和导出。
- [ ] 验证禁用用户立即失去业务访问权限。
- [ ] 验证系统不能降级或禁用最后一名有效管理员。

## 6. 核心业务验收

- [ ] 新增物品后库存、规格、状态和操作记录一致。
- [ ] 入库、出库、换码和盘点均生成正确记录，失败时不会留下部分写入。
- [ ] 并发执行两次库存操作，确认事务不会造成负库存或数量漂移。
- [ ] 老师只能看到自己的操作记录；管理员可以按预期查看全量记录。
- [ ] 采购凭证只接受 JPG、JPEG、PNG，超过 10 MiB 或错误后缀必须失败。
- [ ] 非上传者不能直接读取采购凭证；需要共享的管理员权限符合既定授权模型。
- [ ] 删除采购凭证后，数据库记录和云存储文件符合预期的删除/软删除策略。
- [ ] 库存导出和记录导出的字段、数量、权限范围及中文显示正确。
- [ ] 定时清理只处理过期临时凭证，不删除正式凭证。
- [ ] 在生产配置下调用 `resetTestData` 必须被拒绝。

## 7. 隐私与数据治理

- [ ] 在微信公众平台配置用户隐私保护指引，覆盖姓名、openid、库存记录和采购凭证。
- [ ] 对每类数据明确用途、保存期限、访问角色、删除方式和用户联系渠道。
- [ ] 明确数据库备份和云存储备份的保留期限，到期后同步删除。
- [ ] 日志不记录完整 openid、文件地址、采购内容、访问令牌或原始服务端错误。
- [ ] 测试、演示、截图和公开问题单只使用合成数据。
- [ ] 完成组织适用的法律、合同和未成年人数据处理审查。

## 8. 监控、备份与回滚

- [ ] 为云函数错误率、超时、调用量、存储量和费用设置告警。
- [ ] 设置数据库与云存储备份，并在非生产环境实际完成一次恢复演练。
- [ ] 保存上一稳定版本的提交 SHA、函数版本和安全规则版本。
- [ ] 准备回滚步骤：暂停小程序发布、回滚云函数、恢复安全规则、恢复数据。
- [ ] 明确安全事件联系人；漏洞只通过 GitHub 私密漏洞报告或内部私密渠道处理。

## 9. 发布签署

- [ ] 微信开发者工具的真机预览、体验版和目标机型测试均通过。
- [ ] 业务验收人确认库存与导出结果。
- [ ] 安全复核人确认权限、隐私、依赖和回滚证据。
- [ ] 发布负责人确认提交 SHA、发布时间和回滚负责人。
- [ ] 发布后重新执行管理员、老师和未授权用户的关键冒烟测试。

只有以上适用项目均完成并有私密证据留档后，才把体验版提交审核或发布生产版本。
