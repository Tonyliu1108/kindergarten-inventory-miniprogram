# 公开前安全审计

审计日期：2026-07-22

依赖复核日期：2026-07-28

## 范围

- 当前工作区和全部 Git 提交历史中的常见密钥、令牌、私钥、口令及部署标识；
- 云函数身份、角色与资源归属检查；
- 数据库和云存储访问边界；
- 输入长度、数量和破坏性测试工具保护；
- 前端日志、云函数错误返回和最小化数据返回；
- npm 直接依赖与传递依赖告警；
- 公开仓库中的项目名称、示例资源和本地配置。

## 已完成加固

- 将 AppID、云环境 ID 和本地配置移出公开追踪文件；
- 将具体园区名称替换为通用名称，删除未使用的 quickstart 组件和示例图片；
- 默认禁止微信搜索索引内部库存页面，并关闭非必要的用户访问追踪；
- 移除前端调试响应日志、原始 openid 返回和服务端原始错误返回；
- 收紧用户/申请列表字段，补充关键文本和数值上限；
- 为定时清理函数校验可信触发器，并禁止客户端调用；
- 将测试数据清理改为默认关闭，要求管理员、环境开关和独立确认口令；
- 补充数据库、云存储和云函数安全规则基线；
- 升级稳定版 `wx-server-sdk`，锁定可修复的高风险传递依赖。
- 将 `@cloudbase/database` 声明的 `lodash.set@4.3.2` 统一替换为
  `lodash@4.18.1` 的安全 `set` 实现，并在 CI 中验证安装结果、原型污染防护和
  `npm audit`；
- 增加最小权限 GitHub Actions，持续校验语法、JSON、锁文件和 SDK 初始化。

## 已知边界

- 腾讯云开发 SDK 的 `@cloudbase/database@1.4.3` 仍声明
  `lodash.set@4.3.2`。本项目在每个云函数中使用 npm override，将该传递依赖安装为
  `lodash@4.18.1`；锁文件和 CI 会阻止回退到易受影响的独立包。
- 本项目不使用数据库 `watch`/实时监听接口。CI 会拒绝新增 `.watch()` 调用；若未来
  需要实时监听，必须先重新评估上游模块的导入兼容性和服务端字段路径信任边界，不能
  直接删除该检查。
- 仓库规则文件不会自动修改云端配置，部署者必须在控制台应用并测试。
- 静态检查不能替代微信开发者工具和真实云环境中的权限、上传、导出及定时触发验证。
- 任何历史重写或新仓库发布都必须以不包含旧部署标识和旧示例资源的干净根提交为起点。

安全审计降低了已识别风险，但不构成绝对安全保证。
